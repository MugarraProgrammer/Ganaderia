spring:
  application:
    name: ganaderia
  profiles:
    active: dev

server:
  address: 0.0.0.0
  port: ${PORT:8082}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka}
