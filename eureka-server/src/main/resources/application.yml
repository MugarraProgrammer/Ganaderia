spring:
  application:
    name: eureka-server

server:
  address: 0.0.0.0
  port: ${PORT:8761}

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
